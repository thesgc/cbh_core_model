============
Installation
============

At the command line::

    $ easy_install cbh_core_model

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv cbh_core_model
    $ pip install cbh_core_model
